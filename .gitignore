# The landing page's live demo is generated from the binary at deploy time,
# so it can never drift from the product it demonstrates. Never committed.
/site/demo.html

# Compiled binary + release artifacts
/marginalia
/dist/
*.exe

# Test / coverage output
*.out
*.test
coverage.txt

# Local review artifacts created while testing against docs in this repo
/*.feedback.jsonl

# OS / editor cruft
.DS_Store
*.swp
