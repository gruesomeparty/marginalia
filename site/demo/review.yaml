title: Ingest rework — sign-off
instructions: >
  Sign off on the ingest rework. The retry path is the part I am least sure
  about — everything else is mechanical.
actions:
  - type: blocker
    label: Blocker
    key: b
  - type: nit
    key: n
notes:
  - block: "1.2/2"
    text: the cap came from the queue's batch limit — is that still right?
